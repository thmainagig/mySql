<!DOCTYPE html>
<html>
<head>
	<title>Working with mySql</title>
</head>
<body>
	<?php 
	// A database is a collection of tables that store related data
	// One database for one application 
	// Access permissions are granted at database level 
	// table is a set of columns and rows
	// A table represents a single concept (noun) products, orders, patients
	// You create relationships between tables
	// A column is a set of data of a single simple type
	// Columns have types eg like strings, integer, varchar
	// A row is a single record of data eg 
	// Field is an intersection of column and a row
	// An index is data structure on a table used to increase look up speed
	// a foreign key is a table column whose value reference row in another table
	// The foundation of relational database
	?>
</body>
</html>